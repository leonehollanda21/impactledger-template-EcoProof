# EcoProof Frontend

Vue 3 + Vite + Vue Router 4 + Pinia. Consome a API em `http://localhost:8000/api/v1`.

## Rodar
```
npm install
npm run dev
```

Tokens JWT persistidos em `localStorage` (`ecoproof_token`, `ecoproof_role`).
